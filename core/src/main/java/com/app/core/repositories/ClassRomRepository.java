package com.app.core.repositories;

import com.app.core.models.ClassRom;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClassRomRepository extends JpaRepository<ClassRom, Integer> {
}
